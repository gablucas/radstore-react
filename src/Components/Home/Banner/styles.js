import styled from "styled-components";

export const Container = styled.section`
    position: relative;

    p {
        position: absolute;
        font: var(--font2-48-b);
        color: #FFFFFF;

        top: 10%;
        left: 30%;
    }

    img {
      width: 100vw;
    }
`